describe('Client Tests', function() {
	console.log('Please write some tests; I\'m lonely');
	it('should have tests', function() {
		expect(true).to.be.true;
	});
	it('should have more than 1 test', function() {
		expect(1 + 1).to.equal(2);
	});
});
